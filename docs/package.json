{
  "name": "amplib-docs",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "start": "parcel src/*.html src/**/*.html --no-cache",
    "build": "parcel build src/*.html src/**/*.html --no-cache --no-scope-hoist"
  },
  "devDependencies": {
    "@parcel/transformer-worklet": "^2.16.4",
    "parcel": "^2.16.4"
  }
}
