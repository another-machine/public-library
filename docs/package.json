{
  "name": "amplib-docs",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "parcel src/*.html src/**/*.html --no-cache",
    "build": "parcel build src/*.html src/**/*.html --no-cache"
  },
  "devDependencies": {
    "@parcel/transformer-worklet": "^2.12.0",
    "parcel": "^2.12.0"
  }
}
